<section>
  <%= form_with model: Ride.new do |f| %>
<div>
  <%= f.label :duration %>
  <%= f.text_field :duration, class: "block col-12 field" %>
</div>
<%= f.submit 'Submit', class: "mt2 btn btn-primary mb1 bg-teal" %>
  <% end %>
</section>
